## pangolin up client

Start a client connection

### Synopsis

Bring up a client tunneled connection

```
pangolin up client [flags]
```

### Options

```
  -d, --detached                 Run in detached mode (background)
      --dns string               DNS server (default: 8.8.8.8)
      --enable-api               Enable API (default: true)
      --endpoint string          Client endpoint (defaults to hostname from config)
  -h, --help                     help for client
      --holepunch                Enable holepunching (default: false)
      --http-addr string         HTTP address
      --id string                Client ID (required)
      --interface-name string    Interface name (default: olm)
      --log-file string          Path to log file (required when using -d)
      --log-level string         Log level (default: info)
      --mtu int                  MTU (default: 1280)
      --ping-interval string     Ping interval (default: 5s)
      --ping-timeout string      Ping timeout (default: 5s)
      --secret string            Client secret (required)
      --socket-path string       Socket path (default: /var/run/olm.sock)
      --tls-client-cert string   TLS client certificate path
      --version string           Version (default: 1)
```

### Options inherited from parent commands

```
      --orgId string   Organization ID
```

### SEE ALSO

* [pangolin up](pangolin_up.md)	 - Start a client or site

###### Auto generated by spf13/cobra on 3-Nov-2025
